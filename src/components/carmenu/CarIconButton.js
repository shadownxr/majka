import { styled } from '@material-ui/core/styles';
import Button from '@material-ui/core/Button';

const CarIconButton = styled(Button)({
  color: 'black',
  maxWidth: '200px',
  maxHeight: '50px',
  padding: '0px 0px 0px 0px',
});


export default CarIconButton